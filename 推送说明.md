# GitHub 推送说明

## 媒体文件处理

**不向 GitHub 推送媒体文件**（图片、视频、音频），媒体统一放到腾讯云 COS。

### 腾讯云 COS 地址

| 项目 | 值 |
|------|------|
| 媒体根地址 | `https://s-class-1403296481.cos.ap-chengdu.myqcloud.com/s-class/` |
| 存储桶 | `s-class-1403296481`（成都 ap-chengdu） |
| 路径前缀 | `s-class/` |

---

## 首次配置（只做一次）

在项目根目录的 `.cos-config.json` 里填入你的腾讯云密钥：

```json
{
  "SecretId": "你的 SecretId",
  "SecretKey": "你的 SecretKey",
  "Bucket": "s-class-1403296481",
  "Region": "ap-chengdu",
  "CosPrefix": "s-class/"
}
```

> 获取密钥：腾讯云控制台 → 访问管理 → API 密钥管理  
> **此文件已被 .gitignore 忽略，不会被推送到 GitHub。**

---

## 完整自动化流程

配置好密钥后，你只需要做一件事：**正常写代码、commit、push**。其余全自动。

### 你做的事

```
写 HTML（媒体用本地路径）→  git add .  →  git commit  →  git push
```

### 自动发生的事

| 时机 | 自动执行 | 做了什么 |
|------|---------|---------|
| `git commit` 时 | **pre-commit 钩子** | 扫描所有 HTML，把 `src="photo.jpg"` 等本地路径 → 替换为腾讯云 COS 完整地址，改好的 HTML 自动加入提交 |
| `git push` 时 | **pre-push 钩子** | 扫描项目中所有媒体文件，增量上传到腾讯云 COS（只上传新增或有变化的），上传完成后才推送到 GitHub |

**你不需要**：
- 手动改任何媒体链接
- 手动上传任何文件到腾讯云
- 记住腾讯云地址

---

## 手动运行（可选）

如果想单独执行某一步：

```bash
# 预览哪些 HTML 会被改链接
node scripts/rewrite-html-media.js --dry

# 执行改链接
node scripts/rewrite-html-media.js

# 预览哪些媒体会被上传
node scripts/upload-media-to-cos.js --dry

# 执行上传
node scripts/upload-media-to-cos.js

# 强制上传所有媒体（不做增量判断）
node scripts/upload-media-to-cos.js --all
```

---

## 一句话总结

**写 HTML 用本地路径 → commit 时链接自动改成腾讯云 → push 时媒体自动上传到腾讯云 → 全程无需手动操作。**

---

## 这样做的目的

- 减小仓库体积，加快 clone/pull
- 媒体走 CDN，访问更快更稳定
- 便于单独管理媒体资源
